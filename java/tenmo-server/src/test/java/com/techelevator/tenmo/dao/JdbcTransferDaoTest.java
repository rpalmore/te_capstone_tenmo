/*package com.techelevator.tenmo.dao;

import com.techelevator.tenmo.model.TransferDTO;

import java.math.BigDecimal;

import static org.junit.Assert.*;

public class JdbcTransferDaoTest {

    @org.junit.Test
    public void createTransfer_check_balance_greater_than_amount_return_true() {
        BigDecimal amount = new BigDecimal(55.00);
        BigDecimal balance = new BigDecimal(1000.00);
        TransferDTO transferDTO = new TransferDTO(1001, 1002, amount);
        JdbcTransferDao transfer = new JdbcTransferDao()

        int response = amount.compareTo(balance); // this is cheating because values are hard-coded. BUT ...
        // should return -1;

        boolean actual = transferDTO.createTransfer()



    }
}*/

    // This is example code only.
   /* @Test
    public void testCalculateDeliveryTotal_sameDay_true_and_zip_21000() {
        FlowerShopOrder order = new FlowerShopOrder("standard", 10);

        BigDecimal actual = order.calculateDeliveryTotal(true, "21000");
        BigDecimal expect = new BigDecimal("9.98");

        Assert.assertEquals(expect, actual);
    }*/